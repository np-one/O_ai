yeah we can integrate with other systems as well and you can target them based on UTM parameters as well so in case it is UTM parameters then you select any channel and in the referral URL you select contains and you just specify the UTM parameter here.,
