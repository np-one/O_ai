The other element that we do <> the other element that we do is automatically is automated redirects. If a user and comes and types a specific thing on the search box which matches the category that you have right <>.,
